<?php
# arrays_loops_tasks: Завдання №15
#
# Дано масив $arr. За допомогою цикла foreach і зміної $count порахуйте кількість єлементів
# цього масиву. Провірте роботу скрипта на прикладі масива з елементами 4, 2, 5, 19, 13, 0, 10.

$count = 0;
$arr = array(4, 2, 5, 19, 13, 0, 10);
foreach ($arr as $value) {
    $count++;
}
echo $count;
